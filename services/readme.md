=> while configuring tsconfig file

- target should esnext or es2020
- module should be nodenext
- open rootdir and outdir

=> npm i -d typescript @types/packagename (to install same packages in ts also so can ts understand)

=> concurrently is use to write/run multiple commands

=> added type:"module" in package.json file

=> to compile and execute the ts js in app
"start": "node dist/server.js",
"dev": "nodemon dist/server.js"

=>
